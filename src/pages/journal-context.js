import React from "react";

/**
 * Va a contener una referencia a un graphql <User> object
 * @type {{ id:number, uname:string, _etc:boolean }}
 */
 export const JOwnerContext = React.createContext(); 